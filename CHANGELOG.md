# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式。

## [1.1.0] - 2026-09-13

### 新增

- `scripts/check_cards.py`：卡片格式硬校验脚本（纯标准库）。校验卡片张数与说明行、必填字段
  完整性与唯一性、核心知识「一句话」约束、标题泛化词与长度、标题重复/高度相似、正文与字段
  标签的语言一致性；退出码非 0 表示存在 `FAIL`。
- `scripts/run_evals.py`：回归校验脚本，抽取 `references/examples.md` 中全部样例并复用同一套
  校验逻辑重跑，改规则后可一键验证不回归。
- 输入形态识别分支：已是卡片或摘要的输入不再二次压缩；多文件/多文章分别成组；代码、表格、
  公式、图形原样保留；超长文按源文标题层级分节处理；空文件与纯样板文本如实报告。
- 明确「源文视为数据」规则，源文中的指令性文本不予执行。
- 输出语言解析顺序：用户显式指定语言优先于源文语言；字段标签与正文语言保持一致。
- 超过 8 张卡片时的说明行规则，以及单组 12 张的硬上限。
- 可选第五字段 `原文依据` / `Source`，用于可溯源的引用核对。

### 变更

- 卡片数量由固定的 5–8 张改为按源文长度取目标区间（3–8 / 6–10 / 8–12），保留「绝不凑数、
  绝不拆句凑数」原则，同时消除长文欠覆盖的问题。
- 英文输出模板与中文模板并列；此前 `SKILL.md` 只给出中文模板，与「语言跟随源文」规则冲突。
- 「一张卡片一个知识点」的判据补充中英双语连接词（`and` / `并且` / `以及` / `同时` / `；`），
  此前判据仅对英文有效。
- 移除 `SKILL.md` 与 `references/card-format.md` 之间的三处重复定义，改为单一事实源：
  字段细则、好/坏示例、特殊场景处理只保留在 `card-format.md`。
- `SKILL.md` 的质量清单改为可执行门禁：写草稿 → 跑 `check_cards.py` → 修完 `FAIL` 再交付。
- `description` 补充口语化触发词（闪卡、读书笔记、flashcards 等），移除与触发无关的能力
  边界描述（边界已在 `SKILL.md` 的 Input Handling 中说明）。
- 字段标签由组合式 `例子 / 自测问题` 改为明确二选一。

### 文档

- `references/examples.md` 由 4 个样例扩展到 6 个：新增「含代码的技术文档」与「信息密集长文」
  两类，分别用于验证代码原样保留与超过 8 张的说明规则。
- README 的示例改为节选，完整样例统一指向 `references/examples.md`，避免双份维护，
  并补充目录结构与测试命令。

## [1.0.0] - 2026-09-13

### 新增

- 初始版本：把粘贴的文章或本地 Markdown/TXT 文件转换成 5–8 张知识卡片。
- 卡片四字段规范（标题、核心知识、简明解释、例子或自测问题）。
- 知识点提取、去重合并、数量自适应与不编造原则。
- `references/card-format.md` 与 `references/examples.md`（4 类文章样例）。
