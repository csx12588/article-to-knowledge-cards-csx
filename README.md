# make-knowledge-cards

把**粘贴的文章**或**本地 Markdown / TXT 文件**转换成 **5–8 张知识卡片**的 Agent Skill。

## 项目解决什么问题

长文章读完就忘，是因为信息密度低、重点被淹没在叙述里。市面上的摘要工具又常犯两个毛病：
**什么都留**（等于没提炼）和**无中生有**（用模型自己的知识补充原文没有的内容）。

本项目提供一个可复用的 Skill，把一篇文章压缩成一组**可复习、可自测**的知识卡片，并且：

- 只保留真正重要的知识点，不按比例平均分配篇幅；
- 一张卡片只讲一个知识点，不重复、不合并不同概念；
- **严格忠实于原文**——不编造原文没有的事实、数字、例子或推断；
- 原文信息不足时**如实减少卡片数量**，而不是硬凑到 5 张。

## 主要功能

| 功能 | 说明 |
| --- | --- |
| 多来源读取 | 支持对话中粘贴的文章文本，以及本地 `.md` / `.markdown` / `.txt` / `.text` 文件 |
| 知识点提取 | 按重要性筛选结论、机制、定义、可执行规则、常见误区，忽略寒暄与营销话术 |
| 去重与合并 | 合并表述不同但含义相同的重复内容；知识点超过 8 个时合并同义面并保留最重要的 |
| 卡片生成 | 每张卡片包含标题、核心知识、简明解释、例子或自测问题 |
| 语言跟随 | 输出语言与原文一致（中文原文出中文卡片，英文原文出英文卡片） |
| 数量自适应 | 通常 5–8 张；信息不足时输出实际数量并附一行说明 |
| 输入边界处理 | 遇到 URL、PDF、图片等不支持的输入时明确说明限制，并给出替代方案 |

### 知识卡片的结构

| 字段 | 说明 |
| --- | --- |
| 标题 | 一个具体、可检索的名词短语，点明该卡片的知识点 |
| 核心知识 | 一句话，只陈述一个事实、结论或规则 |
| 简明解释 | 2–4 句，说明原因、机制或影响 |
| 例子 / 自测问题 | 二选一；概念类优先自测问题，方法类优先取自原文的例子，原文无例子则改用自测问题 |

## 安装方法

本 Skill 不依赖任何第三方包或运行时，只需把技能目录放到宿主的 skills 目录中。

```bash
# 方式一：个人级（对所有项目生效）
cp -r skills/make-knowledge-cards ~/.codebuddy/skills/

# 方式二：项目级（随项目共享，团队内所有人可用）
mkdir -p .codebuddy/skills && cp -r skills/make-knowledge-cards .codebuddy/skills/
```

Windows PowerShell：

```powershell
# 个人级
Copy-Item -Recurse skills\make-knowledge-cards "$HOME\.codebuddy\skills\"

# 项目级
New-Item -ItemType Directory -Force .codebuddy\skills | Out-Null
Copy-Item -Recurse skills\make-knowledge-cards .codebuddy\skills\
```

其他支持 Agent Skills 的宿主同理，放入其约定的 skills 目录即可（目录名必须保持
`make-knowledge-cards`）。

## 使用方法

### 1. 触发技能

用自然语言提出请求即可，例如：

- `把这篇文章做成知识卡片：<粘贴文章内容>`
- `把 ./notes/xxx.md 整理成知识卡片`
- `把这段内容整理成知识卡片，用中文输出`
- `Make knowledge cards from this article.`

### 2. 提供输入

仅支持两种输入来源：

- 直接在对话中**粘贴文章文本**；
- 提供**本地文件路径**，扩展名为 `.md` / `.markdown` / `.txt` / `.text`。

### 3. 获取输出

Skill 会按「知识点提取 → 去重排序 → 选 5–8 张 → 逐张成卡」的流程直接输出卡片，
不输出寒暄或对请求的复述。拿到结果后可直接复制到笔记工具中使用。

## 输入输出示例

### 示例 1：中文科普文章

**输入**

```markdown
# 睡眠如何巩固记忆

记忆的形成大致分为编码、巩固与提取三个阶段。白天学到的信息最初以不稳定的形式暂存在
海马体，只有在睡眠中，海马体会把这些信息"重放"给大脑皮层，信息才会逐渐转移并稳定存储，
形成长期记忆。

睡眠由多个周期组成，每个周期约 90 分钟，包含非快速眼动睡眠（NREM）和快速眼动睡眠
（REM）。深度的慢波睡眠（NREM 第三阶段）对陈述性记忆的巩固最为重要；而 REM 睡眠则更多
与程序性记忆和情绪记忆的整合相关。

这解释了为什么熬夜复习往往事倍功半。睡眠不足还会影响注意力与工作记忆，使编码阶段的
效率下降。
```

**输出**

```markdown
## 知识卡片：睡眠是记忆巩固的关键环节

### 卡片 1：记忆形成的三个阶段
- **核心知识**：记忆的形成分为编码、巩固、提取三个阶段，作用各不相同。
- **简明解释**：编码在学习当下把外界信息转成神经信号；巩固让信息稳定下来；提取是日后
  回忆时调取信息。其中巩固决定信息能否长期留存。
- **自测问题**：记忆形成包含哪三个阶段？分别发生在什么时候？

### 卡片 2：睡眠通过海马体重放完成巩固
- **核心知识**：白天信息先暂存于海马体，睡眠中经"重放"转移到大脑皮层，才形成长期记忆。
- **简明解释**：编码后的信息最初并不稳定，只有在睡眠中由海马体把信息重放给皮层，信息
  才会转移并稳定存储，从而变成长期记忆。
- **自测问题**：为什么说睡眠是记忆巩固不可或缺的一环？

### 卡片 3：睡眠由约 90 分钟一个的周期构成
- **核心知识**：睡眠以约 90 分钟为一个周期，每个周期包含 NREM 与 REM 两种睡眠。
- **简明解释**：睡眠不是均匀的整体，而是在非快速眼动睡眠（NREM）和快速眼动睡眠（REM）
  之间循环。理解周期是理解两类睡眠分工的前提。
- **自测问题**：一个睡眠周期大约多长？周期内包含哪两类睡眠？

（此处省略 3 张卡片：慢波睡眠与 REM 的分工、熬夜复习得不偿失、睡眠不足削弱编码效率）
```

### 示例 2：信息不足时不强行凑数

当原文支撑不了 5 个知识点时，Skill 输出实际数量，并附一行说明：

```markdown
## 知识卡片：2.4.0 版本发布信息

> 原文信息不足以支撑 5 张卡片，以下为 2 张核心卡片。

### 卡片 1：2.4.0 于 3 月 18 日发布并修复移动端登录白屏
...
```

### 更多示例

四类文章的完整输入/输出（中文科普、中文方法、英文技术教程、信息不足的短公告）见
[`skills/make-knowledge-cards/references/examples.md`](skills/make-knowledge-cards/references/examples.md)。

## 能力边界

暂不支持：

- 网页抓取（URL）
- PDF、Word、PPT、图片等二进制文档
- Anki 导出
- 图形界面

遇到不支持的输入时，Skill 会明确说明限制，并建议改为粘贴文本或保存为 `.md` / `.txt`。

## 目录结构

```
.
├── README.md
└── skills/
    └── make-knowledge-cards/
        ├── SKILL.md                  # 技能主文件（工作流、卡片规范、质量规则）
        ├── agents/
        │   └── openai.yaml           # 面向用户的界面元数据（显示名、简介、默认提示）
        └── references/
            ├── card-format.md        # 卡片字段细则、好/坏示例、特殊情况处理
            └── examples.md           # 4 类文章的完整输入/输出测试样例
```

## 测试与验证

- 已用四类文章验证：中文科普、中文方法、英文技术教程、信息不足的短公告（见 `references/examples.md`）。
  测试结果用于完善卡片规范：例子优先取自原文、无例子时改用自测问题；并列规则是否拆卡；
  知识点超过 8 个时的合并策略。
- 使用 skill-creator 自带的校验脚本检查结构（`quick_validate.py` 位于 skill-creator 技能包内）：

  ```bash
  python quick_validate.py skills/make-knowledge-cards
  # Skill is valid!
  ```

## 许可

本项目采用 [MIT License](LICENSE)。
